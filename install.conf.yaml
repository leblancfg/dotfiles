- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bashrc: bashrc
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/Templates: Templates

- shell:
  - [git submodule update --init --recursive, Installing submodules]
